<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.srs.vmwally">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VmWally.fullscreen">
        <activity
            android:name=".dashboard.MainMenuActivity"
            android:exported="true" />
        <activity
            android:name=".dashboard.PinSetActivity"
            android:exported="true" />
        <activity
            android:name=".dashboard.PinScreenActivity"
            android:exported="true" />
        <activity
            android:name=".dashboard.SplashScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true" />
    </application>

</manifest>